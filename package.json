{
  "name": "dripnex-theme-phosphor",
  "version": "0.1.1",
  "private": true,
  "description": "Dripnex theme — Phosphor",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js",
    "pack": "dripnex-plugin pack ."
  }
}
